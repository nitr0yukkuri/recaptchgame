services:
  # Backend (Go)
  - type: web
    name: recaptchgame-backend
    env: go
    region: singapore
    plan: free
    rootDir: backend
    buildCommand: go build -o main .
    startCommand: ./main
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 10000
      - key: GO_VERSION
        value: 1.21

  # Frontend (React + Vite)
  - type: static
    name: recaptchgame-web
    env: static
    region: singapore
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_WS_URL
        # 本番環境用の公開URLを指定（Render側ではこれが優先されます）
        value: wss://recaptchgame-backend.onrender.com/ws